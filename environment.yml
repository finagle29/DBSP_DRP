name: dbsp_drp
channels:
- conda-forge
- defaults
dependencies:
- python >= 3.7
- numpy >= 1.16.0
- astropy >= 4.0
- scipy >= 1.4
- matplotlib >= 3.1
- pyqt >= 5
- pypeit >= 1.1.0
- pytest >= 3.0.7
- yattag >= 1.13
- tqdm
